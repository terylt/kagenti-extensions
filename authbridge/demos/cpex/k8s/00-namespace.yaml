# Demo namespace — self-contained from the rest of kagenti so
# `kubectl delete ns cpex-demo` is a clean tear-down.
apiVersion: v1
kind: Namespace
metadata:
  name: cpex-demo
  labels:
    app.kubernetes.io/part-of: cpex-demo
